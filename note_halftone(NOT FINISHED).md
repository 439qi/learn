# 半色调技术

## 简介

半色调技术是 指用少量的色彩将一幅连续色调图像(如灰度图像和彩色图像)量化为一幅二值图像或是只有少数几种色彩的彩色图像，并且量化后图像在一定距离的视觉效果和原始图像相似的技术 。  
众所周知，数字半色调技术是指基于人眼视觉特性和图像呈色特性，利用数学、计算机等工具，在单色/多色二值呈色设备上实现图像的最优再现的一门技术。  
数字半色调是 利用人眼的低通特性，当在一定距离下观察时，人眼将图像中空间上接近的部分视为一个整体 。利用此特性，人眼观察到的半色调图像局部平均灰度近似于原始图像的局部平均灰度值，从而整体上形成连续色调的效果。

## 连续调图像与半色调图像

## 历史

### 模拟半色调

### 电子半色调

### 数字半色调

## 数字半色调技术

### 抖动法Dithering

点处理算法

1. 算法原理  

    构造`模板/抖动矩阵/阈值矩阵`将图像像素二值化，矩阵的每个元素代表像素的阈值

    $$
        ret(i,j)=\left\{\begin{matrix}
        0, image(i,j)<matrix(i,j)\\
        1, image(i,j)>=matrix(i,j)
        \end{matrix}\right.
    $$  
2. 说明  
    分为随机抖动和有序抖动  
    随机抖动效果很不理想，基本不使用；  
    有序抖动主要有分散型（代表为Bayer有序抖动矩阵）和聚集型（代表为点局部聚簇整体分散）  
    NOT FINISHED
3. 缺点  
    含有明显的周期性纹理

### 误差扩散法Error Diffusion

邻域处理算法

1. 算法原理  
    按从左到右，从上到下的顺序依次对每个像素执行以下操作：  
    - 将像素值由原灰度转换到新的灰度，转换前后的差值称为`误差`，按一定比例将误差加到右方及下方的像素上(`扩散`)
        $$
            ret(i,j) =floor( image(i,j)*\frac{新色彩数}{原色彩数})
        $$
        $$
            delta(i,j) = image(i,j)-ret(i,j)*\frac{原色彩数}{新色彩数}
        $$  

    以上计算过程可以用矩阵描述，称为误差扩散过滤器
2. 说明  
    对于二值图像，上述像素转换的公式相当于128为阈值的二值化，使用其他阈值没有明显的优点，故多数算法仍使用128为阈值  
    一定程度上会使图像边缘模糊，有的过滤器会补偿性的增强边缘

3. 缺点  
    速度相对于抖动法较慢  
    除(0,0)处的像素，其余像素值依赖于相邻像素，无法并行计算

### 迭代法

搜索算法  
NOT FINISHED

1. 算法原理  
2. 说明  
3. 缺点  
    计算量大，复杂度高
